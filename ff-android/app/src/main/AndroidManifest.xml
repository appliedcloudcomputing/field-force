<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.acc.fieldforce" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CustomerList"
            android:theme="@style/CustomTheme" />
        <activity
            android:name=".AddCustomer"
            android:label="Add Customer"
            android:theme="@style/CustomTheme"/>
        <activity
            android:name=".SignUp"
            android:label="@string/title_activity_sign_up">
            android:theme="@style/CustomTheme"
        </activity>
        <activity
            android:name=".Menus"
            android:label="@string/title_activity_menu"
            android:theme="@style/CustomTheme">
        </activity>

        <activity android:name=".Expenses"
            android:theme="@style/CustomTheme"/>
        <activity
            android:name=".LeadsList"
            android:label="@string/title_activity_leads_list"
            android:theme="@style/CustomTheme" >
        </activity>
        <activity
            android:name=".JobListing"
            android:label="@string/title_activity_job_casting"
            android:theme="@style/CustomTheme">
        </activity>
        <activity android:name=".AccountSummary"
            android:theme="@style/CustomTheme"/>
        <activity
            android:name=".JobDescription"
            android:theme="@style/CustomTheme" />
        <activity
            android:name=".ListExpenses"
            android:theme="@style/CustomTheme" />
        <activity
            android:name=".AddLeads"
            android:label="@string/title_activity_add_leads"
            android:theme="@style/CustomTheme">
        </activity>
    </application>

</manifest>
